spring:
  docker:
    compose:
      enabled: true
      file: ../compose.yaml
      lifecycle-management: start_only

  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
