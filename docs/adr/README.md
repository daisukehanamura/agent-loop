# ADR (Architecture Decision Record)

「なぜその選択をしたか」を残す。後から見た自分と、この先のレビュワー（人間でも AI でも）が
同じ判断を再現できるようにするのが目的。

- 迷った末に決めたこと、却下した案がある決定だけ書く。自明な決定は書かない。
- 一度書いた ADR は書き換えず、覆すときは新しい ADR を足して旧版を「却下」にする。
- ファイル名は `NNNN-短い英語スラッグ.md`。
